'use strict';

describe('Controller: OpportunityDetailsIndexCtrl', function () {

  // load the controller's module
  beforeEach(module('hirepoolApp'));

  // var OpportunityDetailsIndexCtrl,
  //   scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   OpportunityDetailsIndexCtrl = $controller('OpportunityDetailsIndexCtrl', {
  //     $scope: scope
  //     // place here mocked dependencies
  //   });
  // }));

  // it('should attach a list of awesomeThings to the scope', function () {
  //   expect(OpportunityDetailsIndexCtrl.awesomeThings.length).toBe(3);
  // });
});
