'use strict';

describe('Controller: ModalsChangePasswordCtrl', function () {

  // load the controller's module
  beforeEach(module('hirepoolApp'));

  // var ModalsChangePasswordCtrl,
  //   scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   ModalsChangePasswordCtrl = $controller('ModalsChangePasswordCtrl', {
  //     $scope: scope
  //     // place here mocked dependencies
  //   });
  // }));

  // it('should attach a list of awesomeThings to the scope', function () {
  //   expect(ModalsChangePasswordCtrl.awesomeThings.length).toBe(3);
  // });
});
