'use strict';

describe('Controller: ModalsAddExperienceCtrl', function () {

  // load the controller's module
  beforeEach(module('hirepoolApp'));

  // var ModalsAddExperienceCtrl,
  //   scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   ModalsAddExperienceCtrl = $controller('ModalsAddExperienceCtrl', {
  //     $scope: scope
  //     // place here mocked dependencies
  //   });
  // }));

  // it('should attach a list of awesomeThings to the scope', function () {
  //   expect(ModalsAddExperienceCtrl.awesomeThings.length).toBe(3);
  // });
});
