'use strict';

describe('Controller: CollaboratorProvideFeedbackIndexCtrl', function () {

  // load the controller's module
  beforeEach(module('hirepoolApp'));

  // var CollaboratorProvideFeedbackIndexCtrl,
  //   scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   CollaboratorProvideFeedbackIndexCtrl = $controller('CollaboratorProvideFeedbackIndexCtrl', {
  //     $scope: scope
  //     // place here mocked dependencies
  //   });
  // }));

  // it('should attach a list of awesomeThings to the scope', function () {
  //   expect(CollaboratorProvideFeedbackIndexCtrl.awesomeThings.length).toBe(3);
  // });
});
